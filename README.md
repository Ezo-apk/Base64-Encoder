# Base 64 Encoder/Decoder

This code is a simple C implementation of a base64 encoder and decoder, complete with an interactive driver code, for use in the command line.

`gcc main.c implementation.c -o encoder`
